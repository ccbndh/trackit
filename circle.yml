machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker-compose build

test:
  override:
    - docker-compose up -d; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:8000/docs
